apiVersion: v1
kind: Pod
metadata:
  name: my-worker-app-pod
  labels:
    author: ravi
    name: worker-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: worker-app
      image: dockersamples/examplevotingapp_worker