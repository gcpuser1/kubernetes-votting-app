apiVersion: v1
kind: Pod
metadata:
  name: my-voting-app-pod
  labels:
    author: ravi
    name: voting-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: voting-app
      image: dockersamples/examplevotingapp_result
      ports: 
        - containerPort: 80